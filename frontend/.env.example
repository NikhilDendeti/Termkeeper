# Base URL of the Django backend's JSON API (no trailing slash).
# Copy this file to .env.local (or .env) and adjust if the backend runs
# somewhere other than the default local dev port.
VITE_API_BASE_URL=http://localhost:8000
